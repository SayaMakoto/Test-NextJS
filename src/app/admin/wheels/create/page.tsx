import CreateWheelClient from "./CreateWheelClient";

export const metadata = {
  title: "Tạo vòng quay - Bảng quản trị",
  description: "Tạo và cấu hình một vòng quay mới."
};

export default function CreateWheelPage() {
  return <CreateWheelClient />;
}
